- *URGENT* - Socket mutex is staying locked when we're trying to read a new message. look into poll().
- *URGENT* - When there are serveral servers on the network and we try to delete servers some are not being taken off the map table.
- Use Singleton Design Pattern instead of what's being used right now.
- Msgs to implement in the following order:
  - JUPE
  - GLINE
- FULLY test Network base.
- Comment the code as needed.
- Write an API documentation.
